export const dynamic = "force-dynamic";
export default function Home() {
  return (
    <main>
      <h1>mystic-api</h1>
      <p>API is live. Try <code>/api/catalog</code> or POST to <code>/api/import/printify</code>.</p>
    </main>
  );
}
